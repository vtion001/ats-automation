/**
 * Popup Components Index
 * Export all popup UI components
 */

// Load all components
// These are loaded before popup.js runs

// Toast Manager
if (typeof window !== 'undefined') {
    window.ToastManagerInstance = null;
}
